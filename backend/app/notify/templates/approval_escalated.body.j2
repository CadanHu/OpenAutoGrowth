An approval task has been ESCALATED to you because the previous role
({{ previous_role }}) didn't respond within their SLA.

Escalation level:  L{{ level }}
Rule:              {{ rule_name }} ({{ rule_code }})
Stage:             {{ stage }}
Campaign ID:       {{ campaign_id }}

Please review and decide as soon as possible — if this also expires the
case will be auto-resolved per the rule's default decision.

  {{ public_url }}/#/governance

— OpenAutoGrowth (SLA scanner)
